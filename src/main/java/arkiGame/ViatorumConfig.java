package arkiGame;

@l0raxeo.arki.engine.mainApp.AppConfig(
        windowTitle = "Viatorum Simulator",
        windowWidth = 1000,
        windowHeight = 1000,
        resizeable = true
)
public class ViatorumConfig {}